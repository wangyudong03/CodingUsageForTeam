name: Build and Release Coding Usage Extension

on:
  push:
    tags:
      - 'v*.*.*'  # Match version tags, like v1.0.0

permissions:
  contents: write
  packages: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: CodingUsage/package-lock.json
        
    - name: Install dependencies
      run: npm ci
      working-directory: CodingUsage
        
    - name: Compile TypeScript
      run: npm run compile
      working-directory: CodingUsage
      
    - name: Install vsce
      run: npm install -g vsce
      
    - name: Package extension
      run: vsce package
      working-directory: CodingUsage
      
    - name: Get version from tag
      id: get_version
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
      
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref_name }}
        name: Release ${{ steps.get_version.outputs.VERSION }}
        body: |
          ## Coding Usage v${{ steps.get_version.outputs.VERSION }}
          
          Monitor your Coding AI usage statistics directly in Cursor/Trae. This extension displays your current billing period usage information in the status bar.

          ### Features
          - Real-time monitoring of Cursor/Trae AI usage
          - Display of current billing period and usage statistics
          - Easy configuration through VS Code settings
        files: |
          CodingUsage/*.vsix
        draft: false
        prerelease: false
        
    # Auto-publish to VSCode Marketplace
    - name: Publish to VSCode Marketplace
      run: vsce publish -p ${{ secrets.VSCE_PAT }}
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      working-directory: CodingUsage
      
    # Auto-publish to OpenVSX Registry
    - name: Install ovsx
      run: npm install -g ovsx
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      
    - name: Publish to OpenVSX Registry
      run: ovsx publish *.vsix -p ${{ secrets.OVSX_PAT }}
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      working-directory: CodingUsage
